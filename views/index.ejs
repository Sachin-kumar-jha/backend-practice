<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Title</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>

    <div class="main w-full min-h-screen bg-zinc-900">
        <h1 class="text-3xl  text-white text-center py-4 font-semibold tracking-tighter">My practice on Backend!</h1>
        <div class="form p-10 text-white">
            <form action="/create" method="POST">
                <input class="block w-full rounded px-3 py-4 bg-zinc-700 outline-none" name="title" type="text"
                    placeholder="enter title " />
                <textarea class="w-full rounded mt-2 resize-none bg-zinc-700 outline-none px-2" name="details"
                    placeholder="Write your task details"></textarea>
                <input class="mt-2 bg-blue-600 rounded px-6 py-1" value="Create task" type="submit" />
            </form>
        </div>
        <div class="tasks p-10 flex justify-center gap-4 flex-wrap">
            <% if (files.length>0) {%>
                <%files.forEach((val)=>{%>
                    <div class="task w-72  px-3 py-4 bg-zinc-800 rounded">
                        <h1 class="text-white font-semibold tex-3xl tracking-tighter">
                            <%=val%>
                        </h1>
                        <div class="flex justify-between items-center mt-3">
                            <a class=" text-blue-500 inline-block " href="/files/<%=val%>">Read More...</a>
                            <a class="text-zinc-400 block " href="/edit/<%=val%>">Edit</a>
                            <a class="text-zinc-400 block " href="/delete/<%=val%>">Delete</a>

                        </div>

                        <p class="font-semibold"></p>
                    </div>

                    <% }) %>
                        <% }else{%>
                            <h3 class="text-zinc-600 px-10">No Tasks Yet</h3>
                            <% } %>
        </div>
    </div>

</body>

</html>